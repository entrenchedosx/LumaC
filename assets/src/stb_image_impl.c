/*
 * Sole stb_image implementation translation unit (PART B/C policy:
 * third-party code is built in exactly one place). Only PNG/JPEG
 * decoding is used by the asset pipeline, but the full header stays
 * vendored unmodified for provenance.
 */
#define STB_IMAGE_IMPLEMENTATION
#include "../../third_party/stb/stb_image.h"
